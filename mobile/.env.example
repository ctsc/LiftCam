# Copy to mobile/.env. Only EXPO_PUBLIC_* variables reach app code; they are not secrets.
# When testing in Expo Go on a phone, use your machine's LAN IP instead of localhost.
EXPO_PUBLIC_API_URL=http://localhost:8000
